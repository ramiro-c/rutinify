Sos el Architect / Intent Enframing Agent. Tu contrato completo está en tu system prompt:
respetalo, incluido el Protocolo de Ambigüedad.

EL PROBLEMA ORIGINAL está en {{bet_file}}. Leelo primero. Es el ÚNICO archivo que necesitás
leer: no explores el repo, no globbees, no busques más contexto.

PRODUCÍ UNA SOLA COSA: un objeto JSON con la entrada de Bet Register completa, con esta forma
exacta:

{
  "id": "{{bet_id}}",
  "status": "draft",
  "owner_governor": "<string>",
  "created_date": "YYYY-MM-DD",
  "deadline": "YYYY-MM-DD",
  "hipotesis": "<Creemos que X provocará Y, lo cual sabremos que es cierto cuando observemos Z>",
  "learning_objective": "<la pregunta que esta Bet existe para responder>",
  "generation_target": {
    "codigo_dominio": "<string>",
    "tests": "<string>",
    "documentacion": "<string>",
    "schema_migraciones": "<string>",
    "configuracion_despliegue": "<string>"
  },
  "resolution_signal": [
    { "metrica": "<string>", "baseline": "<string>", "umbral_exito": "<string>",
      "fuente_instrumentacion": "<string>", "piso_minimo_muestra": "<string o null>" }
  ],
  "risk_envelope": {
    "blast_radius": "<string>",
    "etapas_canary": [
      { "etapa": 1, "exposicion": "<string>", "dwell_time_minimo": "<string>",
        "condicion_de_avance": "<string>" }
    ],
    "triggers_rollback": [
      { "metrica": "<string>", "umbral": 0.0, "comparador": ">", "ventana": "<string>",
        "accion": "automatic_rollback" }
    ]
  }
}

Reglas duras de tipos (el JSON se valida contra el schema del workflow spec):
- `etapa` es entero >= 1. `umbral` es número. `comparador` ∈ ">", ">=", "<", "<=", "==".
- `accion` es exactamente "automatic_rollback". `created_date` y `deadline` son fechas YYYY-MM-DD.
- `etapas_canary` y `triggers_rollback` llevan al menos un elemento. `resolution_signal` también.

Regla de campos ajenos: si un campo ya tiene valor en {{bet_file}}, copialo tal cual y no lo
cambies — `owner_governor`, `created_date` y `deadline` son del humano. Si alguno falta, poné
el literal "<A COMPLETAR POR EL HUMANO>". Nunca inventes el apetito de riesgo: si el Risk
Envelope del archivo no alcanza, dejá el campo con el literal y explicalo en tu reporte.

SI VOLVISTE DESDE UN REDIRECT, no re-emitas la misma Bet: el Validator ya demostró que la
superficie generada no responde al generation_target tal como está formulado. Reformulá la
`hipotesis`, el `learning_objective` o el `generation_target` a la luz de esa evidencia, y
dejá constancia en el Learning Objective de qué cambió respecto de la formulación anterior.

CONTEXTO DE REDIRECT:
{{redirect_context}}

PROTOCOLO DE SALIDA (obligatorio): leé {{bet_file}}, pensá, y devolvé EXCLUSIVAMENTE el objeto
JSON — sin texto antes ni después, sin fences de markdown, sin volver a leer archivos.
